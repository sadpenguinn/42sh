-c[set max mounts between checks]
-C[set mount count]
-e[set error behavior]
-f[force tune2fs to complete even in the face of errors]
-g[set the group which can use reserved filesystem blocks]
-i[interval between checks]
-j[add an ext3 journal to the filesystem]
-J[journal options]
-l[list the contents of the filesystem superblock]
-L[set the volume label of the filesystem]
-m[set the percentage of reserved filesystem blocks]
-M[set the last-mounted directory for the filesystem]
-o[mount options]
-O[set or clear filesystem features]
-r[set the number of reserved filesystem blocks]
-s[set sparse super feature]
-T[set time last checked]
-u[set user who can use reserved filesystem blocks]
-U[set filesystem UUID]
