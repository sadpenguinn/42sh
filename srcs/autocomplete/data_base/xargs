-p[prompt user for each command]
--interactive[prompt user for each command]
-t[verbose - echo each command]
--verbose[verbose - echo each command]
-x[exit if max size exceeded]
--exit[exit if max size exceeded]
-E[specify EOF marker]
-I[specify replacement string for command line]
-L[call program for every number of lines]
