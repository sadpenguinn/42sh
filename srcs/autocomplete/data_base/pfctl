-F[flush the filter parameters specified by modifier]
-A[load only the queue rules present in the rule file]
-D[define macro to be set to value]
-d[disable the packet filter]
-e[enable the packet filter]
-f[load the rules contained in a file]
-g[include output helpful for debugging]
-h[help]
-i[restrict the operation to the given interface]
-K[kill all of the source tracking entries originating from the specified host or network]
-k[kill all of the state entries originating from the specified host or network]
-m[merge in explicitly given options]
-N[load only the NAT rules present in the rule file]
-n[do not actually load rules, just parse them]
-O[load only the options present in the rule file]
-o[control the ruleset optimizer]
-p[use the device file device instead of the default /dev/pf]
-q[only print errors and warnings]
-R[load only the filter rules present in the rule file]
-r[perform reverse DNS lookups on states when displaying them]
-s[show the filter parameters specified by modifier]
-T[specify the command to apply to the table]
-t[specify the name of the table]
-v[produce more verbose output]
-x[set the debug level]
-z[clear per-rule statistics]
