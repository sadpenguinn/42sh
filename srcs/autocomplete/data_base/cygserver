-f[use specified file as config file (default /etc/cygserver.conf)]
-c[number of cleanup threads to use (default 2)]
-p[size of process cache]
-r[number of request threads to use (default 10)]
-d[log debug messages to stderr]
--debug[log debug messages to stderr]
-e[log to stderr (default if stderr is a tty)]
--stderr[log to stderr (default if stderr is a tty)]
-E[don't log to stderr (see -y, -Y options)]
-l[verbosity of logging (1..7) (default 6)]
-y[log to syslog (default if stderr is no tty)]
--syslog[log to syslog (default if stderr is no tty)]
-Y[don't log to syslog (see -e, -E options)]
-m[don't start XSI Shared Memory support]
-q[don't start XSI Message Queue support]
-s[don't start XSI Semaphore support]
-S[shutdown the daemon]
--shutdown[shutdown the daemon]
-h[output usage information and exit]
--help[output usage information and exit]
-v[output version information and exit]
--version[output version information and exit]
