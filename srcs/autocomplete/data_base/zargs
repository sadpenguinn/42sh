--exit[exit if the size (see --max-chars) is exceeded]
-x[exit if the size (see --max-chars) is exceeded]
--help[print summary and exit]
--interactive[prompt before executing each command line]
-p[prompt before executing each command line]
-r[do nothing if there are no input arguments before the eof-str]
--null[split each input-arg at null bytes, for xargs compatibility]
-i[substitute replace-str in the initial-args by each initial-arg]
--verbose[print each command line to stderr before executing it]
-t[print each command line to stderr before executing it]
--version[print the version number of zargs and exit]
