-q[quiet mode]
-l[list all available updates]
--list[list all available updates]
-d[download to directory set in InternetConfig]
--download[download to directory set in InternetConfig]
-i[install (requires root)]
--install[install (requires root)]
--ignored[show or manage ignored updates list (per-user)]
--schedule[scheduler preferences (per-user)]
-h[print command usage]
--help[print command usage]
