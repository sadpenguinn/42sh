-N[create a set]
--create[create a set]
-X[destroy specified set (or all if none specified)]
--destroy[destroy specified set (or all if none specified)]
-F[flush specified set (or all if none specified)]
--flush[flush specified set (or all if none specified)]
-E[rename set]
--rename[rename set]
-W[swap two sets]
--swap[swap two sets]
-L[list the entries and bindings for the specified set (or all if none specified)]
--list[list the entries and bindings for the specified set (or all if none specified)]
-S[save the given set (or all if none specified)]
--save[save the given set (or all if none specified)]
-R[restore a saved session generated by --save from stdin]
--restore[restore a saved session generated by --save from stdin]
-A[add an IP to a set]
--add[add an IP to a set]
-D[delete an IP from a set]
--del[delete an IP from a set]
-T[test whether an IP is in a set]
--test[test whether an IP is in a set]
-H[print help and settype specific help if settype specified]
--help[print help and settype specific help if settype specified]
-q[suppress any output to stdout and stderr]
--quiet[suppress any output to stdout and stderr]
-t[list set names and headers without entries]
--terse[list set names and headers without entries]
-r[enforce DNS name lookup of addresses, slow!]
--resolve[enforce DNS name lookup of addresses, slow!]
--exist[ignore errors when creating an identical set, adding existing entry or deleting a non-existent one]
--timeout[set the timeout for this entry. set must have been created with timeout support]
--timeout[set the timeout for this entry. set must have been created with timeout support]
-s[sorted output]
--sorted[sorted output]
-n[name output only; suppresses showing of set information]
--name[name output only; suppresses showing of set information]
