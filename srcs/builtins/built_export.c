#include "builtins.h"

/*
static int		parse_flags(char **av, char **env)
{
	if (!ft_strcmp(av[1], "-n"))
	{
		nil
	}
	if (!ft_strcmp(av[1], "-p"))
	{
		if (!av[2])
	}
	return (SHERR_OK);

}

int 			built_export(char **av, char **env)
{
	if (av[1])
		parse_flags(av, env);
	else
		print_str_arr(env);
	return (SHERR_OK);
}

int				built_export(char **av, char **env)
{
	char	*key;
	char	*value;

	env = NULL;
	if (!parse_flags(av, &key, &value))
		return (SHERR_ERR);
	ssetenv(key, value);
	return (SHERR_OK);
}*/
